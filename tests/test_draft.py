def test():
    """
    This test is required by pytest to return success code
    For empty tests pytest returns exit code 5,
    which fails the pipeline (see: https://docs.pytest.org/en/7.1.x/reference/exit-codes.html)

    Remove it, when real tests are added
    """
    assert 2 + 2 == 4
